# Yielding. On way to implement blocks in our custom methods is with the yield keyword/method
# The number or arguments at invocation needs to still match the definition, blocks aside.
# yield executes the block.
# yield is used to inject different code into the method. 
